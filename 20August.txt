//before few i have done this problem in O(n^2) complexity but this code will make O(n)
class Solution {
    public boolean isBalanced(TreeNode root) {
       return dfs(root)!=-1;
    }
    public int dfs(TreeNode node){
      if(node==null)
      return 0;
      int l=dfs(node.left);
      if(l==-1)return -1;
      int r=dfs(node.right);
      if(r==-1)return -1;
      int max=Math.abs(l-r);
      if(max>1)return -1;
      return Math.max(l,r)+1;
    }
}
------------------------------------------
class Solution {
    public String reverseOnlyLetters(String s) {
        StringBuilder sb=new StringBuilder();
        char arr[]=s.toCharArray();
       int i=0,j=s.length()-1;
       while(i<j){
          while(i<j&&!(Character.isAlphabetic(s.charAt(i)))){
            i++;
          }
          while(i<j&&!(Character.isAlphabetic(s.charAt(j)))){
            j--;
          }
          char ch=arr[i];
          arr[i]=arr[j];
          arr[j]=ch;
          i++;
          j--;
       }
       for(char ch:arr){
        sb.append(ch);
       }
       return sb.toString();

    }
    
}
---------------------------------------------